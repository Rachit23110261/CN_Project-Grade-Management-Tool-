# Server Configuration
PORT=5000
NODE_ENV=development

# HTTPS Configuration
# Set to 'true' to enable HTTPS, 'false' for HTTP
USE_HTTPS=false

# SSL Certificate Paths (only required if USE_HTTPS=true)
# Leave empty to use default paths: server/ssl/cert.pem and server/ssl/key.pem
# SSL_CERT_PATH=path/to/cert.pem
# SSL_KEY_PATH=path/to/key.pem

# Database Configuration (PostgreSQL)
# For Docker deployment - use service names
DB_HOST=database
DB_PORT=5432
DB_NAME=grade_management
DB_USER=postgres
DB_PASSWORD=password

# Alternative: Database URL format
# DATABASE_URL=postgresql://postgres:password@database:5432/grade_management

# For local development (uncomment these and comment Docker DB config above):
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=grademanagement
# DB_USER=postgres
# DB_PASSWORD=your_password

# JWT Configuration
# IMPORTANT: Generate a strong random secret with at least 32 characters
# Use this command to generate one:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_change_in_production_please_use_long_random_string

# Frontend URL (for email links)
# For Docker deployment - use localhost with mapped port
FRONTEND_URL=http://localhost

# Email Configuration (for forgot password feature)
# For Gmail: Use App Password (not regular password)
# Steps to get App Password:
# 1. Enable 2-Step Verification in Google Account
# 2. Go to Security > App Passwords
# 3. Generate password for "Mail" app
# 4. Copy the 16-character password (remove spaces)
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_gmail_app_password_here
